var names = ["Taylor", "Marcia", "Kamille", "Amber", "Travis"];
	var text = "";
    
    for (var i = 0; i < names.length; i++) {
    	text += names[i] + "<br>";
    }

	var names = ["Taylor", "Marcia", "Kamille", "Amber", "Travis"];
	var text = "";
	var x = 0;
    
    while (x < names.length) {
    	text += names[x] + "<br>";
    	x++;
    }
var x = 0;

	do {
		text += names[x] + "<br>";
    	x++;

	} while (x < 10);